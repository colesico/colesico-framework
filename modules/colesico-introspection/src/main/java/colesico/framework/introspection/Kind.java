package colesico.framework.introspection;

/**
 * Introspected element kind
 */
public enum Kind {
    CLASS,
    INTERFACE,
    ENUM,
    ANNOTATION,
    CONSTRUCTOR,
    METHOD,
    FIELD,
    ENUM_CONSTANT,
    PARAMETER
}
